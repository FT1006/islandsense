{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn src.islandsense.api:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/dashboard",
    "restartPolicyType": "ON_FAILURE"
  }
}
